pipeline {
    agent any
    stages {
        
       stage('Deploy App') {
      steps {
        script {
          kubernetesDeploy(configs: "web-ui-app.yaml", kubeconfigId: "kubecfg")
        }
      }
    }
    
    }
   
}
