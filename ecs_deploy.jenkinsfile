pipeline {
    agent any
    stages {
        
       stage('Deploy App') {
      steps {
        script {
          kubernetesDeploy(configs: "spring-boot/web-ui-app.yaml", kubeconfigId: "kubecfg")
        }
      }
    }
    
    }
   
}
